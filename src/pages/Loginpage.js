import React from 'react';
import Login from '../components/Login';  // Import the form

const SignUpPage = () => {
  return (
    <div>
      <h1>Enter user and pass to login</h1>
      <SignUp />
    </div>
  );
};

export default LoginPage;